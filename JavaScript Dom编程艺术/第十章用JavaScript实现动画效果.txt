*第十章主要属于过程信息，需要的是多操作和内在化，比喻法，模型纠错和图表法 练习

-我以前看过或听过这个知识吗？
 有

-我理解知识的含义吗？（至少是字面上的意思。）
	哈哈，这章完成时间有点慢啊!不过这章确实让人感到js的强大，
 如其名，js动画效果。
	这章主要介绍了setTimeout和clearTimeout函数
 第一个函数可以在浏览器加载完成后
 以毫秒的时间执行函数var x =setTimeout("函数()",time);
 第二个函数清除掉setTimeout的执行动作clearTimeout(x);
 当循环中有事件时，也是循环完调用事件
 当事件触发时候获取到的for循环中的局部变量往往是最后一个
 clearTimeout函数可以把消除动画滞后效果
 
 
-我知道知识从何而来，与哪些知识有关系吗？
	css雪碧可以利用position:absolute绝对定位来完成，在图片位置设
 置div外层。然后给div设置图片切割中的每张宽高，设置为相对定位
 position:relative，原div位置转移。
 由于图片相对于div位置，所以左上角坐标为0,0；向下和后坐标为负数
    由于循环对于事件的支持度为循环完毕后调用，所以时间事件不能用
 循环改用递归。使用递归调用，让图片坐标递增实现移动效果
	关于setTimeout("函数()",time);由于函数被设置为字符串，所以里
 面的参数需要实行字符串拼接。注意变量字符串拼接是将变量中的值拼接
 的，所以参数如果本身就是字符串的情况下，需要用引号包裹起来，这里
 使用了转义字符
 var x = "move(\"" + message + "\"," + left + "," + top + "," + time + ")";
 
 
-我删除了那些不恰当的联系吗？我删除了那些错误结论吗？

-我将知识用到实际生活中了吗？