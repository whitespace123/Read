*第十一章主要属于过程信息，需要的是多操作和内在化，比喻法，模型纠错和图表法 练习

-我以前看过或听过这个知识吗？
 有

-我理解知识的含义吗？（至少是字面上的意思。）
	这章介绍了一些html5的特性，html5的功能确实很强大，更是做了一
 行为层和表现层的工作。
	1<canvas>可以创建画布图像，利用js的事件可以做成动画，canvas的
 原点是在左上角
 做canvas的灰度处理时候分三个步骤，首先制作和源图片一致的画布，再
 获取源图片的rgb值，将其变为灰度数据，最后将灰度数据放进画布中。
	2<video><audio>可以创建视频和音频，不过每个浏览器的兼容性不同
 所以需要给浏览器做兼容性代码
	3html5还提供了一些表单元素，相比较之前的input type要更加简便
 和智能化，但是也带来了兼容性的问题
 
 
-我知道知识从何而来，与哪些知识有关系吗？
	addEventListener这个函数为事件监听器，当事件调用的时候，可以
 多次启动事件。
 事件监听器，第一个参数为事件名，第二个函数为函数名 如("mouseover", myFunction)会执行事件范围不止一次的函数
 
 
-我删除了那些不恰当的联系吗？我删除了那些错误结论吗？

-我将知识用到实际生活中了吗？