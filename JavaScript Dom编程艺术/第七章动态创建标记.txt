*第七章主要属于过程信息，需要的是多操作和内在化，比喻法，模型纠错和图表法 练习

-我以前看过或听过这个知识吗？
 有

-我理解知识的含义吗？（至少是字面上的意思。）
 首先介绍了write方法，这个方法作用是替换html页面中的所有元素
 然后是innerHtml属性，可以替换所调用节点对象的所有元素子节点
 createElement方法和createTextNode方法是创造元素节点对象和文
 本节点对象。 appendChild方法是给调用元素节点对象添加节点对象
 调用方必须为元素节点对象，如document.createElement("p").cre
 -ateTextNode("Hello"); insertBefor方法是调用者为父元素节点，
 第一个参数为要插入的元素节点，第二个参数为在哪个元素节点之前
 插入
 然后介绍了Ajax技术，实现不刷新全部页面也能和服务器交互，实现的
 对象为XMLHttpRequest，IE浏览器比较特殊需要照顾
 同源策略：就是所属网址要是来自于一个域、协议、端口
 最后说了一些HAjax，渐进增强Ajax，其实就是在没有Ajax技术的时候
 能够达到效果，不依靠但是有更好
 
-我知道知识从何而来，与哪些知识有关系吗？
 在js创造节点对象的时候，它们像是"孤儿"一般，我们需要将它们加入
 到DOM的大家庭里，虽然它们不会显示在HTML文档中
 write比较蛮狠，他能直接改变页面，InnerHtml则是改变父元素中的子
 元素，不过他们的方式和java的servlet比较相似
 
-我删除了那些不恰当的联系吗？我删除了那些错误结论吗？

-我将知识用到实际生活中了吗？